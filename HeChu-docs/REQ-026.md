# REQ-026: 修复 Web 前端服务配置缺失

**日期**: 2026-02-16  
**状态**: ✅ 已完成  
**关联需求**: REQ-023 (Web UI), REQ-025 (Docker 部署)

## 📋 需求概述

修复 Blazor WebAssembly 前端的启动配置问题，前端因缺少必要的服务注册导致无法连接到后端 API。

## 🎯 问题现象

- 前端部署后打开页面报错
- 无法连接到后端服务
- 浏览器控制台显示依赖注入失败

## 🔍 根本原因

[Program.cs](../src/EndfieldPuzzleSolver.Web/Program.cs) 缺少必要的服务配置：

### 缺失的配置

1. **未注册 `PuzzleApiClient` 服务**
   - 前端页面通过 `@inject PuzzleApiClient Api` 依赖注入
   - 但 DI 容器中没有注册该服务
   - 导致运行时抛出依赖注入异常

2. **未配置 `HttpClient` 的 `BaseAddress`**
   - `PuzzleApiClient` 依赖 `HttpClient` 发送请求
   - 但没有配置 `BaseAddress` 指向后端 API 地址
   - 导致 HTTP 请求失败

3. **未注册 FluentUI 服务**
   - 前端页面使用 `@inject IToastService Toast`
   - 但没有调用 `AddFluentUIComponents()` 注册服务
   - 导致 `IToastService` 等接口无法解析

4. **未读取 `appsettings.json` 配置**
   - `wwwroot/appsettings.json` 中配置了 `ApiBaseUrl`
   - 但 `Program.cs` 没有读取该配置
   - 导致后端地址配置无效

### 原有代码（错误）

```csharp
using EndfieldPuzzleSolver.Web;
using Microsoft.AspNetCore.Components.Web;
using Microsoft.AspNetCore.Components.WebAssembly.Hosting;

var builder = WebAssemblyHostBuilder.CreateDefault(args);
builder.RootComponents.Add<App>("#app");
builder.RootComponents.Add<HeadOutlet>("head::after");

await builder.Build().RunAsync();
```

## 🔧 解决方案

### 修复后的代码

```csharp
using EndfieldPuzzleSolver.Web;
using EndfieldPuzzleSolver.Web.Services;
using Microsoft.AspNetCore.Components.Web;
using Microsoft.AspNetCore.Components.WebAssembly.Hosting;
using Microsoft.FluentUI.AspNetCore.Components;

var builder = WebAssemblyHostBuilder.CreateDefault(args);
builder.RootComponents.Add<App>("#app");
builder.RootComponents.Add<HeadOutlet>("head::after");

// 读取配置
var appSettings = builder.Configuration.GetSection("App").Get<AppSettings>() ?? new AppSettings();

// 配置 HttpClient 指向后端 API
builder.Services.AddScoped(sp => 
{
    var client = new HttpClient { BaseAddress = new Uri(appSettings.ApiBaseUrl) };
    return client;
});

// 注册 API 客户端
builder.Services.AddScoped<PuzzleApiClient>();

// 注册 FluentUI 服务
builder.Services.AddFluentUIComponents();

await builder.Build().RunAsync();
```

### 修复说明

1. **读取配置**: 从 `appsettings.json` 的 `App` 节读取 `ApiBaseUrl`
2. **配置 HttpClient**: 设置 `BaseAddress` 为后端 API 地址
3. **注册 API 客户端**: 将 `PuzzleApiClient` 注册为 Scoped 服务
4. **注册 FluentUI**: 调用 `AddFluentUIComponents()` 注册所有 FluentUI 服务

### 配置文件

[wwwroot/appsettings.json](../src/EndfieldPuzzleSolver.Web/wwwroot/appsettings.json) 中配置后端 API 地址：

```json
{
  "App": {
    "ApiBaseUrl": "https://your-backend-api-url/"
  }
}
```

## ✅ 验证结果

### 编译状态
- ✅ 编译成功 (0 错误, 2 警告)
- ⚠️ 警告: `ConfigurationBinder.Get<T>` 的 AOT 兼容性警告（不影响功能）

### 功能验证（需重新部署后验证）
- [ ] 前端页面正常加载
- [ ] 可以连接到后端 API
- [ ] 打开截图功能正常
- [ ] 粘贴截图功能正常
- [ ] 示例加载功能正常
- [ ] Toast 提示正常显示

## 📦 后续步骤

### 重新发布前端

```bash
# 1. 发布前端到 wwwroot
dotnet publish src/EndfieldPuzzleSolver.Web/EndfieldPuzzleSolver.Web.csproj -c Release -o publish/web

# 2. 部署到 GitHub Pages
# 将 publish/web/wwwroot 的内容推送到 gh-pages 分支
```

### 验证部署

1. 访问已部署的前端地址
2. 打开浏览器开发者工具 (F12)
3. 检查 Console 是否有错误
4. 检查 Network 标签，确认 API 请求指向正确地址
5. 测试上传截图功能

## 📚 相关文件

### 修改文件
- [src/EndfieldPuzzleSolver.Web/Program.cs](../src/EndfieldPuzzleSolver.Web/Program.cs) - 添加服务配置

### 相关文件（未修改）
- [src/EndfieldPuzzleSolver.Web/wwwroot/appsettings.json](../src/EndfieldPuzzleSolver.Web/wwwroot/appsettings.json) - API 地址配置
- [src/EndfieldPuzzleSolver.Web/Services/PuzzleApiClient.cs](../src/EndfieldPuzzleSolver.Web/Services/PuzzleApiClient.cs) - API 客户端实现
- [src/EndfieldPuzzleSolver.Web/AppSettings.cs](../src/EndfieldPuzzleSolver.Web/AppSettings.cs) - 配置类定义
- [src/EndfieldPuzzleSolver.Web/Pages/Index.razor](../src/EndfieldPuzzleSolver.Web/Pages/Index.razor) - 主页面（使用依赖注入）

## 💡 经验总结

### Blazor WebAssembly 启动配置检查清单

1. ✅ **Root Components**: 添加 `App` 和 `HeadOutlet`
2. ✅ **HttpClient**: 配置 `BaseAddress` 指向 API 地址
3. ✅ **API Client**: 注册自定义 API 客户端服务
4. ✅ **UI Components**: 注册 UI 组件库服务（如 FluentUI）
5. ✅ **Configuration**: 读取 `appsettings.json` 配置

### 常见错误

- ❌ 忘记注册依赖注入中使用的服务
- ❌ HttpClient 没有配置 BaseAddress
- ❌ 配置文件存在但未读取
- ❌ UI 组件库服务未注册

### 调试技巧

1. 浏览器 F12 Console 查看 JavaScript 错误
2. Application 标签查看静态资源加载
3. Network 标签查看 API 请求地址
4. 检查 `_framework/blazor.boot.json` 确认配置文件是否打包

## 🔗 相关需求

- REQ-023: Web UI 视觉优化（前端界面开发）
- REQ-024: Web 拖拽功能修复（前端功能增强）
- REQ-025: Docker 容器化部署（后端部署）
